{
  "coda": {
    "stats": true,
    "logs": true
  },
  "operations": {
    "match": {
      "entrypoint": true,
      "action": "string.match.regex",
      "params": {
        "value": "i LIKE big BUTTS and I cannot LIE",
        "regex": "^[a-zA-Z ]+$"
      },
      "store": "match"
    }
  }
}
